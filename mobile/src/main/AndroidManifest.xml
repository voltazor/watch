<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.yalantis.watch">

    <uses-sdk tools:node="replace"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MobileMainActivity"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_mobile_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".RecyclerViewActivity"
            android:screenOrientation="portrait"
            android:label="@string/lbl_recycler_view"/>

        <activity
            android:name=".VolumeControlActivity"
            android:screenOrientation="portrait"
            android:label="@string/lbl_volume_control"/>

    </application>

</manifest>
